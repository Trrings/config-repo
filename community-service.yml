server:
  port: 8105
  servlet:
    context-path: /community-service

#GRPC config
grpc:
  server:
    port: 9005
  client:
    authority: localhost:9001
spring:
  profiles:
    active: dev
  config:
    import:
      - optional:classpath:/common.yml
